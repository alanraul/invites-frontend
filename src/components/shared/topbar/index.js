// Libraries
import React, { Component } from 'react'

class Topbar extends Component {
  /**
   * Render principal.
   * @returns {nav}
   */
  render () {
    return (
      <nav className='navbar is-dark'>
        <a className='navbar-item has-text-white title'>Invitaciones</a>
      </nav>
    )
  }
}

export default Topbar
